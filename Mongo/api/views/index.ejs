<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Contacts</title>
    <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
    <h1>My contacts</h1>
    <div class="border">
        <p class="right"><a href="/form">Add contact</a></p>
        <div clear="clear"></div>
        <%  dbcontacts.forEach((row, index)=>{ %>
                <div class="width-30 border left ">
                    <div class="right">
                        <a href="/fetch/<%= row._id %>">Update</a>
                        <a href="/delete/<%= row._id %>">Delete</a>
                    </div>
                    <h4><%= index+1 %> </h4>
                    <p> <%= row.name %> </p>
                    <p> <%= row.email %> </p>
                    <p> <%= row.mobile %> </p>
                </div>
                <div class="clear"></div>
        <% }) %>    
    </div>    
</body>
</html>